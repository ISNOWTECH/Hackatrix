<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App.Views.Message">
    <ContentView.Content>
        <Frame BackgroundColor="{Binding CorloMessage}">
            <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="{Binding Align}">
                <Label Text="{Binding Message}" Style="{StaticResource Message}"/>
                <Label Text="{Binding Date}" Style="{Static Date}"/>
            </StackLayout>
        </Frame>
  </ContentView.Content>
</ContentView>